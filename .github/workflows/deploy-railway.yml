name: 🚀 Deploy to Railway

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
      
    - name: 🐳 Setup Flutter with Docker
      uses: subosito/flutter-action@v2
      with:
        channel: 'stable'
        cache: true
        
    - name: 📦 Get Flutter dependencies
      run: |
        cd aegis_app
        flutter pub get
        
    - name: 🔨 Build Flutter web
      run: |
        cd aegis_app
        flutter build web --web-renderer canvaskit --no-sound-null-safety
        
    - name: 📦 Install Railway CLI
      run: npm install -g @railway/cli
      
    - name: 🚀 Deploy to Railway
      env:
        RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
      run: |
        cd aegis_app/build/web
        railway up --service=$RAILWAY_SERVICE_ID
        
    - name: ✅ Deploy status
      run: |
        echo "🎉 Deploy completed!"
        echo "🌐 App URL: https://realtimeoworldmonitor-production.up.railway.app"
